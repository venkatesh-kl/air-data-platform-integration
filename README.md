# data platform integration

1. copy the `.env.example` to `.env` & setup the client secrets.
2. install dependencies by `pnpm install`
3. run `pnpm start` to see the data fetched in real time
4. Check [src/test/ts](./src/test.ts) for the base changes.

## Model

[AIR DP Model](./src/air-dp-model.ts)
